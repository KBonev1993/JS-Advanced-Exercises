function positions(input){
    let result = '';
    for (let index = 0; index < input.length; index += 2) {
            result += input[index];
            result += " "
        }
        console.log(result); 
    }
